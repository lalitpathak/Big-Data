create Database Olist; 	
use Olist;

-- Create table Product_category

CREATE TABLE Olist.products_category (UID varchar(50) COLLATE utf8_unicode_ci,product_category_name varchar(50) COLLATE utf8_unicode_ci,  product_category_name_english varchar(50) COLLATE utf8_unicode_ci  );
